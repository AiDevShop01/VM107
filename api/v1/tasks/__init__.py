"""Phase 48 Plan 48-09 — task polling endpoint package.

GET /api/v1/tasks/get_task?task_id=X is the polling endpoint for in-flight
or completed refinement tasks. Plain JSON, no streaming (CONTEXT § Decision 17).
"""
