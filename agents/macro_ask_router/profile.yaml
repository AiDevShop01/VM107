# Agent-internal profile — referenced from registry/agent_profile/vm107.macro_ask_router.yaml
# Phase 94-06 — PURE CLASSIFIER per §J. NEVER answers. NEVER imports LLM clients.
agent_id: vm107.macro_ask_router
domain: Routing
phase: 94

constitutional_skills:
  - citation-discipline
  - never-answers

# lookup_capability is the sole permitted tool — registry-driven specialist discovery.
allowed_tools:
  - lookup_capability

denied_tools:
  - belief_store.propose
  - vm102_forecast_result_emit
  - vm102_pillar_compute
  - trade_execution_tool
  - code_execution_tool
  - filesystem_write
  - call_subordinate
  - narrative_emit         # §J — router NEVER emits narrative

memory_scope:
  account_scope: NONE
  execution_scope: NONE
  cross_trade_visibility: NONE
  narrative_visibility: NONE

max_iterations: 1
max_cost_usd: 0.0
latency_budget_ms: 500

# Invoked on demand by the synthesizer / dashboard — no event subscription.
trigger_events: []
event_importance_floor: LOW

emits:
  - conversation_plan       # internal dict; not a contract YAML — schema enforced via tests

consumed_capabilities:
  - lookup_capability
