# Agent-internal profile — referenced from registry/agent_profile/vm107.executive_summary.yaml
# Phase 94-06 — event-driven narrative composer (cached, HIGH+ only).
agent_id: vm107.executive_summary
domain: ExecutiveSummary
phase: 94

constitutional_skills:
  - citation-discipline
  - narrative-only-explain

allowed_tools:
  - lookup_capability
denied_tools:
  - belief_store.propose
  - vm102_forecast_result_emit
  - vm102_pillar_compute
  - trade_execution_tool
  - code_execution_tool
  - filesystem_write
  - call_subordinate

memory_scope:
  account_scope: NONE
  execution_scope: NONE
  cross_trade_visibility: NONE
  narrative_visibility: NONE

max_iterations: 1
max_cost_usd: 0.0       # Wave 3b is template-based; 94-07 may swap in LLM completion
latency_budget_ms: 1500

# Event subscription matrix per §K.3 — REGEN ONLY on HIGH+ events.
trigger_events:
  - macro_release
  - central_bank
  - regime_change
  - forecast_update
event_importance_floor: HIGH

emits:
  - executive_summary_section

consumed_capabilities:
  - pillar_engine
  - pillars_section
  - regime_state
