schema_version: 2
title: trade_auditor_agent
description: >
  Per-trade critique agent. Produces a structured NarrativeEnvelope for a single
  execution. Orchestration entry point only — this top-level profile does NOT
  invoke subordinates; that is done by MentorPipelineOrchestrator (CTX-§3 LOCKED).
parent_profile: null

allowed_tools: []
denied_tools:
  - call_subordinate
  - code_execution_tool
  - trade_execution_tool

constitutional_skills:
  - citation-discipline
optional_skills:
  - critique-discipline
  - narrative-structure
  - pattern-recognition
allowed_skills:
  - citation-discipline
  - critique-discipline
  - narrative-structure
  - pattern-recognition

memory_scope:
  account_scope: "required"
  execution_scope: "required"
  cross_trade_visibility: "NONE"
  narrative_visibility: "NONE"

max_iterations: 5
max_cost_usd: 0.50

input_contract: fingpt_core.contracts.narrative.reader_io.ReaderInput
output_contract: fingpt_core.contracts.narrative.envelope.NarrativeEnvelope
