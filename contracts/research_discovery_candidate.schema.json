{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "VM107/contracts/research_discovery_candidate.schema.json",
  "title": "ResearchDiscoveryCandidate",
  "description": "Phase 92 Plan 05 — emitted by vm107.research_discovery_agent when cross-doc analysis surfaces a novel pattern. Routed to Phase 91 UAE under category=research_discovery.",
  "type": "object",
  "required": [
    "discovery_id",
    "pattern_summary",
    "supporting_docs",
    "indicators",
    "severity",
    "category"
  ],
  "properties": {
    "discovery_id": {
      "type": "string",
      "description": "Idempotency key — UUID v4 generated by the discovery agent."
    },
    "pattern_summary": {
      "type": "string",
      "description": "Human-readable summary of the discovered pattern."
    },
    "supporting_docs": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 3,
      "description": "≥3 ResearchDocument.document_id values supporting the pattern."
    },
    "indicators": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "FRED EconomicIndicator IDs the pattern is anchored to."
    },
    "sources": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Distinct source_ids (≥2 required upstream)."
    },
    "severity": {
      "type": "string",
      "enum": ["watch", "warning", "blocking"],
      "description": "Internal severity. Phase 91 UAE translates to Info / Important / Critical."
    },
    "category": {
      "type": "string",
      "const": "research_discovery"
    }
  },
  "additionalProperties": false
}
