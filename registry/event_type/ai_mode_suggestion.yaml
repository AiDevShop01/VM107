id: ai_mode_suggestion
type: event_type
status: real
shipped: 72
planned_phase: null
category: cognitive_lifecycle
version: '1.0'
producer: vm100
payload_schema: fingpt_core.contracts.state_engine.events.AiModeSuggestionPayload
retention: permanent
pii: false
replayable: true
emitted_by: state_engine_service
tags:
  - phase-72
  - cognitive_lifecycle
hard_scoped: true
# Phase 73-followup deploy-fix: Stage 2 CapabilityRegistry validator requires
# `impact_on_decision` on event_type entries. Phase 72-02 shipped this YAML
# without the field; old container images had a looser validator. New rebuild
# fails Stage 2 here. MEDIUM is the conservative impact level — AI mode is an
# advisory signal, the deterministic state machine still makes the final call.
impact_on_decision: MEDIUM
last_changed: 2026-05-31
deprecated: false
