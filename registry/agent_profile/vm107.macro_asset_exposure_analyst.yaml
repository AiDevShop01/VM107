id: vm107.macro_asset_exposure_analyst
type: agent_profile
status: deprecated
shipped: 85
last_changed: "2026-06-13"
name: vm107.macro_asset_exposure_analyst
description: >-
  Phase 85 — per-asset BULL/BEAR/NEUTRAL + strength + confidence from rolling
  correlation matrix. Invoked in parallel with macro_release_analyst after a
  macro.release_events Redis message fires. Produces per-asset exposure rows
  written to economic_event_asset_exposure table. REQ-85-2 (60s budget),
  REQ-85-4 (B1 reasoning artifact), REQ-85-5 (citations), REQ-85-7 (router
  fail-fast). Primary tool: vm102.indicator_asset_exposure.
profile_path: VM107/profiles/macro_asset_exposure_analyst.yaml
allowed_tools:
  - vm102.indicator_asset_exposure
  - vm102.indicator_history
  - vm107.lookup_reasoning_artifact
  - lookup_capability
denied_tools:
  - call_subordinate
  - code_execution_tool
  - trade_execution_tool
max_iterations: 5
max_cost_usd: 0.25
impact_on_decision: HIGH
hard_scoped: false
deprecated: true
tags:
  - agent-profile
  - macro
  - phase-85
  - event-driven
  - asset-exposure

# P167C (167-05) — additive governance block; existing scalars above are UNCHANGED (D-05).
# SKELETON (D-03, implement-or-retire) — decision record, profile NEVER deleted (09 §5).
# Grammar frozen in Documentation/Agent Zero/agent-catalogue/AGENT-CONTRACT-BLOCK-SCHEMA.md
agent_contract:
  contract_version: 1
  catalogue_ref: macro/macro-asset-exposure-analyst.md
  status: retired                            # RETIRED Phase 173 (D-01) — deprecate-in-place; top-level status: deprecated drops it from dispatch, profile file KEPT (09 §5)
  authority: recommend                       # declared ceiling; no belief_store.propose granted; unbuilt (no authority exercised today)
  trigger_normalized: [event-driven]         # intended Phase-85 macro.release_events fan-out; no shipped trigger scalar (04 §A)
  memory_rw:
    reads: [rolling_correlation_matrix, indicator_history]     # intended
    writes: [economic_event_asset_exposure_rows, b1_reasoning_artifact]  # intended — nothing written today
  collaboration:
    invoked_by: [phase85_release_fanout]     # intended — nothing invokes it today (unbuilt)
    may_invoke: []
    may_challenge: []
  failure_behavior:
    insufficient_evidence: "(intended) thin correlation window -> abstain with limitation"
    stale_data: "(intended) surface matrix as-of/staleness"
    conflict: "(intended) divergent exposures presented, not averaged"
    tool_failure: "actual: agent is absent — dispatcher must reject the profile gracefully (AGENT-1)"
  evaluation:
    correctness_kpi: "implement-or-retire decision recorded = 1.0 (skeleton catalogued, not silently dropped, D-03)"
    cost_slo: max_cost_usd
    latency_slo: "n/a — no latency_budget_ms on this profile"
    honesty: "fabrication_rate=0; degradation_surfaced=1.0 (skeleton never reported as built)"
