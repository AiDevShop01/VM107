id: vm107.macro_release_analyst
type: agent_profile
status: deprecated
shipped: 85
last_changed: "2026-06-13"
name: vm107.macro_release_analyst
description: >-
  Phase 85 event-driven release analyst — produces What Happened + Why It Matters
  + regime_impact_label for a single FRED release. Invoked by Wave-3 Brain fan-out
  after a macro.release_events Redis message fires. REQ-85-2 (60s budget), REQ-85-4
  (B1 reasoning artifact), REQ-85-5 (citations), REQ-85-7 (router fail-fast).
  Slot 30 fail-fast + Slot 20 B1 artifact write wired via vm107.macro_* prefix.
profile_path: VM107/profiles/macro_release_analyst.yaml
allowed_tools:
  - vm102.indicator_history
  - vm102.indicator_releases
  - vm102.indicator_distribution
  - vm102.indicator_regime
  - vm107.lookup_reasoning_artifact
  - lookup_capability
denied_tools:
  - call_subordinate
  - code_execution_tool
  - trade_execution_tool
max_iterations: 5
max_cost_usd: 0.25
impact_on_decision: HIGH
hard_scoped: false
deprecated: true
tags:
  - agent-profile
  - macro
  - phase-85
  - event-driven
  - release-analyst

# P167C (167-05) — additive governance block; existing scalars above are UNCHANGED (D-05).
# SKELETON (D-03, implement-or-retire) — decision record, profile NEVER deleted (09 §5).
# Grammar frozen in Documentation/Agent Zero/agent-catalogue/AGENT-CONTRACT-BLOCK-SCHEMA.md
agent_contract:
  contract_version: 1
  catalogue_ref: macro/macro-release-analyst.md
  status: retired                            # RETIRED Phase 173 (D-02, RESOLVED 2026-09-01 was IMPLEMENT) — superseded by built domain-analyst pipeline; deprecate-in-place, profile file KEPT (9 inbound refs, 09 §5)
  authority: recommend                       # declared ceiling; no belief_store.propose granted; unbuilt
  trigger_normalized: [event-driven]         # intended Wave-3 Brain fan-out on macro.release_events; no shipped trigger scalar (04 §A)
  memory_rw:
    reads: [vm102_indicator_history, vm102_indicator_releases, vm102_indicator_distribution, vm102_indicator_regime]  # intended
    writes: [regime_impact_label_result, b1_reasoning_artifact]  # intended — nothing written today
  collaboration:
    invoked_by: [brain_fanout]               # intended — nothing invokes it today (unbuilt)
    may_invoke: []
    may_challenge: []
  failure_behavior:
    insufficient_evidence: "(intended) thin release context -> abstain with limitation"
    stale_data: "(intended) surface VM102 read as-of/staleness"
    conflict: "(intended) present divergent signals, never average"
    tool_failure: "actual: agent is absent — dispatcher must reject the profile gracefully (AGENT-1)"
  evaluation:
    correctness_kpi: "implement-or-retire decision recorded = 1.0 (disposition: implement, D-03)"
    cost_slo: max_cost_usd
    latency_slo: "n/a — no latency_budget_ms on this profile (REQ-85-2 60s SLA)"
    honesty: "fabrication_rate=0; degradation_surfaced=1.0 (skeleton never reported as built)"
