# REGISTER_CAPABILITY: type=agent_profile, id=strategy_refinement_critic, path=VM107/registry/agent_profile/strategy_refinement_critic.yaml
# Phase 48 § Decision 1: bounded refinement evaluator. Transformation-pure;
# never rewrites artifacts. Loaded one family-skill overlay per invocation
# via family_skill_map. Anti-pattern enforcement: profile name is NEVER
# "critic_agent" (semantic bleed from Phase 60 mentor + Phase 46 mesh).
id: strategy_refinement_critic
type: agent_profile
status: real
shipped: 48
last_changed: 2026-05-18
name: strategy_refinement_critic
description: >
  Bounded refinement evaluator. Reads strategy artifacts (StrategySpec,
  CodeModule, BuildReport, BacktestResult) and emits a structured
  CriticVerdict (ACCEPT | REFINE | REJECT) with typed RefinementTarget
  objects. Transformation-pure: never rewrites artifacts. Per-family
  heuristics arrive via the orchestrator-selected SKILL.md overlay.
version: "1.0.0"
phase: 48
parent_profile: null
hard_scoped: false
sub_profiles: []
allowed_tools:
  - search_knowledge
  - document_query
  - response
  - lookup_capability
  - skills_tool
denied_tools:
  - call_subordinate
  - code_execution_tool
  - trade_execution_tool
constitutional_skills: []
optional_skills:
  - breakout-critic
  - mean-reversion-critic
  - trend-continuation-critic
  - auction-rotation-critic
  - volatility-expansion-critic
allowed_skills:
  - breakout-critic
  - mean-reversion-critic
  - trend-continuation-critic
  - auction-rotation-critic
  - volatility-expansion-critic
memory_scope:
  account_scope: NONE
  execution_scope: NONE
  cross_trade_visibility: NONE
  narrative_visibility: NONE
max_iterations: 10
max_cost_usd: 1.0
input_contract: null
output_contract: null
template_version: "1.0.0"
template_version_changelog:
  - version: "1.0.0"
    date: "2026-05-18"
    change: Initial Phase 48 ship — bounded refinement adjudicator profile.
tags:
  - agent-profile
  - critique
  - refinement
  - phase-48
  - bounded-loop
impact_on_decision: HIGH
allowed_agent_profiles: []
deprecated: false
producer: VM107
location:
  source: VM107/agents/strategy_refinement_critic/agent.yaml

# P167C (167-03) — additive governance block; existing scalars above are UNCHANGED (D-05).
# Grammar frozen in Documentation/Agent Zero/agent-catalogue/AGENT-CONTRACT-BLOCK-SCHEMA.md
agent_contract:
  contract_version: 1
  catalogue_ref: critics/strategy-refinement-critic.md
  status: built
  authority: reject                          # veto authority; transformation-pure, no constructive verb
  trigger_normalized: [reactive]             # invoked at aggregation points by the refinement orchestrator; no shipped trigger scalar
  memory_rw:
    reads: [knowledge_base, artifact_under_review]
    writes: []
  collaboration:
    invoked_by: [refinement_orchestrator]
    may_invoke: []
    may_challenge: [idea_agent, strategy_agent]
  failure_behavior:
    insufficient_evidence: "missing artifact fields => REFINE with a 'provide X' target; never ACCEPT on a gap"
    stale_data: "a backtest older than the artifact's freshness rule flagged in the verdict"
    conflict: "contradictory metrics (spec vs backtest) surfaced as a REFINE target, not blended"
    tool_failure: "search_knowledge/document_query down => fail loud, name the tool, escalate; never blind ACCEPT"
  evaluation:
    correctness_kpi: "false-accept rate <= 0.05 (seeded-defect set); false-reject <= 0.15"
    cost_slo: max_cost_usd
    latency_slo: "n/a — no latency_budget_ms on this profile"
    honesty: "fabrication_rate=0; degradation_surfaced=1.0"
