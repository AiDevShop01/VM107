id: vm107.executive_summary_writer
type: agent_profile
status: real
shipped: 85
last_changed: "2026-06-13"
name: vm107.executive_summary_writer
description: >-
  Phase 85 — composes Hero Card 50-word executive summary from release_analyst +
  asset_exposure_analyst outputs. Third and final Phase 85 agent — fires LAST per
  Brain task DAG. WS publish fires AFTER this agent completes (Plan 85-11).
  Composer-only; no VM102 tool re-fetch (upstream agents already fetched this data).
  Graceful degradation on missing upstream: returns low-confidence "Awaiting"
  envelope instead of blocking. REQ-85-2 (60s budget), REQ-85-4 (B1),
  REQ-85-5 (citations), REQ-85-7 (router fail-fast).
profile_path: VM107/profiles/executive_summary_writer.yaml
allowed_tools:
  - vm107.lookup_reasoning_artifact
  - lookup_capability
denied_tools:
  - call_subordinate
  - code_execution_tool
  - trade_execution_tool
  - vm102.indicator_history
  - vm102.indicator_asset_exposure
  - vm102.indicator_releases
  - vm102.indicator_distribution
  - vm102.indicator_regime
max_iterations: 3
max_cost_usd: 0.10
impact_on_decision: HIGH
hard_scoped: false
deprecated: false
tags:
  - agent-profile
  - macro
  - phase-85
  - event-driven
  - executive-summary
  - composer

# P167C (167-06) — additive governance block; existing scalars above are UNCHANGED (D-05).
# Grammar frozen in Documentation/Agent Zero/agent-catalogue/AGENT-CONTRACT-BLOCK-SCHEMA.md
agent_contract:
  contract_version: 1
  catalogue_ref: orchestration/executive-summary-writer.md
  status: skeleton                           # RETIRE candidate — executive_summary (Phase 94) is the built canonical (migration-map §C)
  authority: recommend                       # intended narration ceiling; no propose/compute tools granted
  trigger_normalized: [event-driven]         # no shipped `trigger` scalar — authored fresh per 04 §A (Brain DAG last-node); not wired as a skeleton
  memory_rw:
    reads: [release_analyst_output, asset_exposure_analyst_output, b1_reasoning_artifact]
    writes: []                               # intended hero-card summary + B1; not wired as a skeleton
  collaboration:
    invoked_by: [dagster:phase85_brain_dag]  # intended; not wired as a skeleton
    may_invoke: []
    may_challenge: []
  failure_behavior:
    insufficient_evidence: "missing upstream -> return low-confidence 'Awaiting' envelope, never block (REQ-85-7)"
    stale_data: "stale upstream would flag the section"
    conflict: "conflicting analyst outputs surfaced, not smoothed"
    tool_failure: "an artifact-lookup outage -> 'Awaiting' envelope, named"
  evaluation:
    correctness_kpi: "retire decision resolved (RETIRE in favour of executive_summary, confirmed via AGENT-1 follow-up)"
    cost_slo: max_cost_usd
    latency_slo: "n/a — no latency_budget_ms on this profile (intended 60s budget, REQ-85-2)"
    honesty: "fabrication_rate=0; degradation_surfaced=1.0"
