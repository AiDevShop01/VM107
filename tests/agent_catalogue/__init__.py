# Phase 167 Wave 0 — agent governance test package.
# Home for the agent_contract_lint tests (167-01), boot-validator tests (167-08),
# and constitution-preamble extension tests. Standalone: imports pyyaml only,
# never python-frontmatter and never the VM107 app runtime.
