{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "Provisional Phase 89 schema. Refined by Phase 91 capability registry YAML.",
  "title": "alert_candidate_created",
  "description": "Provisional Phase 89 JSON Schema for Phase 91 alert_candidate_created envelope (Decision 13).",
  "type": "object",
  "required": [
    "event_type",
    "alert_type",
    "producer_agent_id",
    "subject_type",
    "subject_id",
    "severity",
    "b13_internal_severity",
    "confidence",
    "explanation",
    "citations",
    "created_at",
    "schema_version"
  ],
  "additionalProperties": true,
  "properties": {
    "event_type": {
      "type": "string",
      "const": "alert_candidate_created",
      "description": "Fixed envelope discriminator"
    },
    "alert_type": {
      "type": "string",
      "enum": [
        "regime",
        "correlation",
        "discovery",
        "contradiction",
        "macro_indicator",
        "liquidity",
        "price"
      ],
      "description": "Category of alert for Phase 91 routing"
    },
    "producer_agent_id": {
      "type": "string",
      "minLength": 1,
      "description": "Phase 89 agent that produced this candidate (e.g. phase89.macro_investigator)"
    },
    "subject_type": {
      "type": "string",
      "minLength": 1,
      "description": "Entity type being alerted on (e.g. indicator, asset, belief)"
    },
    "subject_id": {
      "type": "string",
      "minLength": 1,
      "description": "Entity identifier (e.g. CPIAUCSL, DXY)"
    },
    "severity": {
      "type": "string",
      "enum": ["Info", "Watch", "Important", "Critical", "RegimeChange"],
      "description": "Phase 91 5-severity taxonomy (user-facing)"
    },
    "b13_internal_severity": {
      "type": "string",
      "enum": ["info", "warning", "blocking"],
      "description": "B13 internal severity that maps to the user-facing severity via Decision 13 boundary translation"
    },
    "confidence": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Agent confidence in this alert candidate [0.0, 1.0]"
    },
    "explanation": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable explanation of why this alert was raised"
    },
    "citations": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Source references supporting the alert"
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 UTC timestamp when alert candidate was created"
    },
    "schema_version": {
      "type": "string",
      "const": "0.1-provisional",
      "description": "Schema version — incremented when Phase 91 stabilises the contract"
    }
  }
}
