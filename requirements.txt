a2wsgi==1.10.8
ansio==0.0.1
browser-use==0.5.11
docker==7.1.0
duckduckgo-search==6.1.12
# Phase 47-07/47.1: pin pyreqwest-impersonate to last version with prebuilt
# aarch64 wheels — duckduckgo-search 6.1.12 has an unbounded constraint and
# uv was resolving to 0.5.5 which fails to compile boring-sys-imp from source.
# 0.5.3 has prebuilt wheels for both aarch64 and x86_64 linux.
pyreqwest-impersonate==0.5.3
faiss-cpu==1.11.0
fastmcp>=3.2,<4  # Phase 67 Plan 12 — pinned 3.x range; Pitfall 10 (API drift) — BearerAuthProvider→JWTVerifier, stateless_http moved to run()
fasta2a==0.5.0
flask[async]==3.0.3
flask-basicauth==0.2.0
flaredantic==0.1.5
GitPython==3.1.43
giturlparse==0.14.0
inputimeout==1.0.4
kokoro>=0.9.2
simpleeval==1.0.3
langchain-core==0.3.49
langchain-community==0.3.19
langchain-unstructured==0.1.6
openai-whisper==20250625
lxml_html_clean>=0.4.0  # CVE-2024-52595 fix: XSS CWE-79 CVSS 8.4
markdown==3.7
mcp>=1.24.0,<2  # Bumped 2026-05-28 from ==1.22.0 — fastmcp 3.2+ requires mcp>=1.24.0; mcp_handler.py uses stable client APIs (ClientSession, stdio_client, sse_client, streamablehttp_client, SessionMessage, CallToolResult, ListToolsResult) that are compatible across 1.22→1.24.
newspaper3k==0.2.8
paramiko==3.5.0
playwright==1.52.0
pypdf==6.0.0
python-dotenv==1.1.0
pytz==2024.2
qdrant-client>=1.12.0
redis>=5.0.0
sentence-transformers==3.0.1
tiktoken==0.8.0
unstructured[all-docs]==0.16.23
unstructured-client==0.31.0
webcolors==24.6.0
nest-asyncio==1.6.0
crontab==1.0.1
markdownify==1.1.0
pydantic==2.11.7
pymongo>=4.7.0
neo4j>=5.15  # Phase 87 — locked 2026-06-15. See .planning/phases/87-.../87-01-PLAN.md (was >=5.0)
pymupdf==1.25.3
pytesseract==0.3.13
pdf2image==1.17.0
crontab==1.0.1
pathspec>=0.12.1
psutil>=7.0.0
soundfile==0.13.1
imapclient>=3.0.1
html2text>=2024.2.26
beautifulsoup4>=4.12.3
boto3>=1.35.0
exchangelib>=5.4.3
pywinpty==3.0.2; sys_platform == "win32"
python-socketio>=5.14.2
uvicorn>=0.38.0
httptools>=0.6,<0.7  # active HTTP parser for uvicorn http="auto"; without it uvicorn falls back to strict h11, which intermittently rejects the WebUI's parallel keep-alive requests with 400 "Invalid HTTP request received" (blank screen). h11 stays installed as the hardened fallback (see security floor below).
watchdog==6.0.0
wsproto>=1.2.0
# Security floor pins for transitive dependencies
# These packages are pulled transitively — floor pins prevent resolver regressions
Pillow>=10.2.0  # security floor: heap buffer overflow CWE-122, eval injection CWE-95, DoS CWE-400
nltk>=3.9.3  # security floor: RCE CWE-94, code injection CWE-94
h11>=0.16.0  # security floor: HTTP request smuggling CWE-444
packaging>=23.2,<25  # AZI-07 boot-crash floor: litellm imports `packaging`; a rebuild that drops this transitive dep bricks litellm boot (crash-loop while health still reads 200) — see memory project_vm107_packaging_boot_defect. Bounded <25 to avoid an unvetted major bump; image resolves 24.2. Verified by VM107/scripts/import_smoke.sh.
urllib3>=2.6.0  # security floor: resource exhaustion CWE-770, data amplification CWE-409
cryptography>=46.0.0  # security floor: insufficient data authenticity CWE-345
werkzeug>=3.0.3  # security floor: RCE CWE-94
# Phase 43.2 — structured output Stage 2 repair
json-repair>=0.59,<1.0
# Phase 85 Plan 01 — psycopg2 direct connection to tradetracker_analytics / tradetracker
# get_analytics_conn() and get_default_conn() in services/postgres_analytics_client.py
psycopg2-binary>=2.9.0
# Phase 87 — Wave 4/5 sibling services + Wave 0 conftest fixtures. Locked 2026-06-15.
# See .planning/phases/87-.../87-01-PLAN.md
apscheduler>=3.10  # Wave 4 macro_story_tracker, Wave 5 macro_regime_monitor schedulers
testcontainers>=3.7  # Wave 0 conftest fixtures (neo4j_test_driver, qdrant_test_client)
